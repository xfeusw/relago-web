# Relago Web

Nix-flake monorepo.

- server/ – Haskell backend
- client/ – React frontend

Each subproject is self-contained and provides its own flake, dev shell, and build.

See subdirectory READMEs for usage.
